# Spotify OpenAPI spec

`openapi.yaml` is a vendored copy of Spotify's OpenAPI schema.
Source: https://developer.spotify.com/reference/web-api/open-api-schema.yaml

Run `./refresh.sh` and `git diff spec/openapi.yaml` to see exactly what
Spotify changed since the last sync of this wrapper.
